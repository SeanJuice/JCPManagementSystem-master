import { CpDocFilterPipe } from './cp-doc-filter.pipe';

describe('CpDocFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CpDocFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
