
export class Tool {
    CatergoryID: string; //need more clarification
    ToolStatusID: string;
    ConditionID: string;
    ToolName: string;
    ToolDateAdded: number; //need to find date variable for this one (e.g. datetime)
    ToolDescription: string;
}